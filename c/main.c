#include <stdio.h>
# include <math.h>
int main()
int x,polynome,s;
    printf("entre la valeur de x : ");
    scanf("%d",&x);
    s = ((((3*x+2)x–5)x–1)x+7)x–6;
    printf("la valeur de s est : %d",s);
    return 0;
}